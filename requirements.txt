# F20: bound every dependency to its current major version so a surprise
# major/breaking (or supply-chain-compromised) release can't be pulled in on a
# fresh deploy. Complete fix = commit a pip-freeze lockfile + run pip-audit in CI.
flask>=3.1,<4
requests>=2.32,<3
python-dotenv>=1.0,<2
google-api-python-client>=2.190,<3
google-auth-oauthlib>=1.3,<2
google-auth-httplib2>=0.2,<1
ddgs>=9.0,<10
dnspython>=2.8,<3
openai>=1.30,<2
resend>=2.0,<3
svix>=1.90,<2
